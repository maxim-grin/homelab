apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  # Headless: the app addresses the pod directly and there is exactly one.
  # A ClusterIP would add a hop and hide which replica answered.
  clusterIP: None
  selector:
    app: postgres
  ports:
    - port: 5432
      name: postgres
